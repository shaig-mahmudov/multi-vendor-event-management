spring.datasource.url=jdbc:mysql://localhost:3306/event_management
spring.jpa.hibernate.ddl-auto=update
spring.profiles.active=local

app.jwt.secret=${JWT_SECRET:bXlzdXBlcnNlY3JldGtleW15c3VwZXJzZWNyZXRrZXkxMjM0NTY3OA==}
app.jwt.expiration-seconds=${JWT_EXPIRATION_SECONDS:3600}

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:multivendoreventmanagement@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nxyd jzce bysb lzwd}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# App base URL
app.base-url=${APP_BASE_URL:http://localhost:8080}

# CORS
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173